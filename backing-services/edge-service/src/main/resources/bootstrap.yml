spring:
  application:
    name: edge-service
  cloud:
    config:
      uri: http://localhost:8081

security:
  oauth2:
    sso:
      loginPath: /api/uaa/login

zuul:
  routes:
    ui:
      path: /ui/**
      url: http://localhost:3000/

server:
  port: 8765