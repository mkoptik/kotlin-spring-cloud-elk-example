server:
  port: 8081

spring:
  application:
      name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///home/mkoptik/projects/kotlin-spring-cloud-elk-example-config

management:
  security:
    enabled: false