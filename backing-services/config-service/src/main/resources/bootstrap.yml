server:
  port: 8081

spring:
  application:
      name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mkoptik/kotlin-spring-cloud-elk-example-config.git

management:
  security:
    enabled: false