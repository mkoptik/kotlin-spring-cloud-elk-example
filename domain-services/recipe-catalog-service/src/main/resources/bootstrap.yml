spring:
  application:
    name: recipe-catalog
  cloud:
    config:
      uri: http://localhost:8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 0